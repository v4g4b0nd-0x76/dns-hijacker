[package]
name = "dns-hijacker"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive", "rc"] }
tokio = { version = "1.52.3", features = ["full"] }
toml = "1.1.2"
