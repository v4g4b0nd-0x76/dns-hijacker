<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  macOS launchd service for Apple Silicon (M1/M2/M3/M4).
  Install (as root, required for UDP/53):
    sudo mkdir -p /opt/dns-relay
    sudo cp target/aarch64-apple-darwin/release/dns-relay /opt/dns-relay/
    sudo cp conf.toml /opt/dns-relay/
    sudo cp assets/com.dns-relay.plist /Library/LaunchDaemons/
    sudo launchctl bootstrap system /Library/LaunchDaemons/com.dns-relay.plist
    sudo launchctl enable system/com.dns-relay
    sudo launchctl kickstart -k system/com.dns-relay
-->
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>com.dns-relay</string>
	<key>ProgramArguments</key>
	<array>
		<string>/opt/dns-relay/dns-relay</string>
	</array>
	<key>WorkingDirectory</key>
	<string>/opt/dns-relay</string>
	<key>RunAtLoad</key>
	<true/>
	<key>KeepAlive</key>
	<true/>
	<key>StandardOutPath</key>
	<string>/var/log/dns-relay.out.log</string>
	<key>StandardErrorPath</key>
	<string>/var/log/dns-relay.err.log</string>
	<key>ProcessType</key>
	<string>Background</string>
	<!-- Port 53 requires root on macOS; run the LaunchDaemon as root. -->
	<key>UserName</key>
	<string>root</string>
</dict>
</plist>
